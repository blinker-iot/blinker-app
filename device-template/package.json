{
  "name": "@blinker/device-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test:embedded": "npm run build && node tests/embedded-load.mjs",
    "lint": "eslint \"src/**/*.ts\" vite.config.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "penpal": "^7.0.6"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vite": "^8.1.5"
  }
}
